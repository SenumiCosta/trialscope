# TrialScope Frontend

React + Vite single-page console for the TrialScope MVP.

```powershell
npm.cmd run dev
```

The dev server proxies `/api` to the backend controller at `http://localhost:4000`.
